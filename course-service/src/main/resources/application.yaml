spring:
  application:
    name: course-service
  datasource:
    url: jdbc:postgresql://postgres:5432/identity-service
    username: postgres
    password: htht01225923050
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

server:
  port: 8002
  servlet:
    context-path: /course
